import { TabPlaceholder } from '../../src/components/navigation/TabPlaceholder';

export default function CartTabScreen() {
  return (
    <TabPlaceholder
      title="Your cart"
      subtitle="Multi-vendor cart groups line items by shop before checkout."
    />
  );
}
