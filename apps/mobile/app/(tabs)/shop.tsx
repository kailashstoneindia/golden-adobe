import { TabPlaceholder } from '../../src/components/navigation/TabPlaceholder';

export default function ShopTabScreen() {
  return (
    <TabPlaceholder
      title="Shop profile"
      subtitle="Update shop details, delivery areas, and business settings."
    />
  );
}
